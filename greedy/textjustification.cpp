#include <bits/stdc++.h>
using namespace std;

vector<string> fullyjustify(vector<string> &words, int maxwidth)
{
    int n = words.size();
    vector<string> v;
    int count = 0;
   
}
int main() {}