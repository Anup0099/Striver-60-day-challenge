#include <bits/stdc++.h> 
using namespace std;
// You are given a binary string s
//  of length n
//  (a string that consists only of 0
//  and 1
// ). A number x
//  is good if there exists a binary string l
//  of length n
// , containing x
//  ones, such that if each symbol si
//  is replaced by si⊕li
//  (where ⊕
//  denotes the bitwise XOR operation), then the string s
//  becomes a palindrome.

// You need to output a binary string t
//  of length n+1
// , where ti
//  (0≤i≤n
// ) is equal to 1
//  if number i
//  is good, and 0
//  otherwise.

// A palindrome is a string that reads the same from left to right as from right to left. For example, 01010, 1111, 0110 are palindromes.
int main() {
    int t;cin>>t;
    while(t--)
    {
        int n;cin>>n;
        string s;cin>>s;
        
        string ans="";
        int count=0;
        

        
        
    }
} 