#include <bits/stdc++.h> 
using namespace std;

int main() 
{
    int n,k,p;
    cin>>n>>k>>p;
    vector<vector<int>> arr(n,vector<int>(n));
    for(int i=0;i<n;i++)
    {
        for(int j=0;j<n;j++)
        {
            cin>>arr[i][j];
        }
    }

    

} 