public static char charEncrypt(char character, int key) {
    //     if (character >= 'a' && character <= 'z') {
    //         return (char) ((character - 'a' + key) % 26 + 'a');// %26 is used to wrap around the alphabets if the key is
    //                                                            // greater than 26 then it will wrap around the alphabets
    //                                                            // and start from the beginning of the alphabets again
    //     } else if (character >= 'A' && character <= 'Z') {
    //         return (char) ((character - 'A' + key) % 26 + 'A');// %26 is used to wrap around the alphabets if the key is
    //                                                            // greater than 26 then it will wrap around the alphabets
    //                                                            // and start from the beginning of the alphabets again
    //     } else {
    //         return character;
    //     }
    // }

    // public static String myName() {
    //     String s = "hello my name is Henry";
    //     String temp = s.substring(s.lastIndexOf(" ") + 1);
    //     s = " " + s + " ";

    // }