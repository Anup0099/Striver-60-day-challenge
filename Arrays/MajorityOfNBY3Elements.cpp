#include <bits/stdc++.h>
using namespace std;
int majority(vector<int>&nums){
    map<int,int>mp;
    int n = nums.size();
    
}
int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);
}